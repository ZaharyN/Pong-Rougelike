TO DO:

- Implement the other upgrades

- Add which player is picking the current upgrades because there is no way to know

------------------------------------------------------------------------------------------------------------------------------------------------------------------
Bugs:
- When the player gets the reduce spin upgrade it has negative spin which is not correct

------------------------------------------------------------------------------------------------------------------------------------------------------------------
Future Optimizations:

1. Make a struct for the card in order to remove all the variables for each card and store a vector<UpgradeCard>. Also, you can add isHovered bool in the struct
and keep track in the UIManger for the lastHovered card. This will prevent resetting the onHover state every frame which I find overkill.

------------------------------------------------------------------------------------------------------------------------------------------------------------------
Ideas:

- Think of how you can show the dash cooldown so the player knows when it is available

- Make the game continue when the ball touches the dead zone. When a player fails to return the ball, the opponent gets a point. The game ends when a player 
  reaches 11 points. This way, there is sense to stack the power ups and they become relevant. The ball's speed should be reset.